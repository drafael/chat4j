name=${project.name}
version=${project.version}
buildTime=${maven.build.timestamp}
copilotOAuthClientId=${chat4j.copilot.oauth.client.id}
codexOAuthClientId=${chat4j.codex.oauth.client.id}
appleTeamId=${chat4j.apple.team.id}
appleBundleId=${chat4j.apple.bundle.id}
appleSignerCn=${chat4j.apple.signer.cn}
